# Models

LangChain supports language models from many providers.

Topics:
- OpenAI models
- HuggingFace models
- Custom LLM wrappers
- Model selection and parameter tuning

Example:
- Use `OpenAI` or `ChatOpenAI` for GPT-based models
- Use `HuggingFaceHub` for open-source models
